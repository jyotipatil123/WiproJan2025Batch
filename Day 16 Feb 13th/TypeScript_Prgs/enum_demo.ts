enum Direction {
    Up,
    Down,
    Left,
    Right
  }

  let direction: Direction = Direction.Up;

  if (direction === Direction.Up) {
    console.log("We are going up!"); // Output: We are going up!
  }
    
